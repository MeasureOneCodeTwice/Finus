The `services` and `webserver` folders contain all the files to deploy Finus microservices and webserver respectively. 
To run a service 
1. `cp scripts/*` to either `services` or `webserver`
2. in whichever folder you chose run `./start.sh`
